"""
mkdocs-gen-files script.

This project keeps the API reference content in `docs/api/index.md`.
We include this file so the MkDocs `gen-files` plugin can be enabled without
failing the build if/when we decide to auto-generate API pages in the future.
"""

# Intentionally a no-op for now.
